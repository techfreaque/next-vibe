import type { IconComponent } from "../../../web/lib/helper";
import { makeCliIcon } from "./make-icon";

const symbol = "G"; // eslint-disable-line i18next/no-literal-string
export const SiGoogle: IconComponent = makeCliIcon(symbol);
