import type { IconComponent } from "../../../web/lib/helper";
import { makeCliIcon } from "./make-icon";

const symbol = "𝕏"; // eslint-disable-line i18next/no-literal-string
export const SiX: IconComponent = makeCliIcon(symbol);
