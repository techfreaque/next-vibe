import type { IconComponent } from "../../../web/lib/helper";
import { makeCliIcon } from "./make-icon";

const symbol = "𝐼"; // eslint-disable-line i18next/no-literal-string
export const Italic: IconComponent = makeCliIcon(symbol);
