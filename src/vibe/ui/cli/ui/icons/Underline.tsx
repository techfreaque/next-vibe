import type { IconComponent } from "../../../web/lib/helper";
import { makeCliIcon } from "./make-icon";

const symbol = "U̲"; // eslint-disable-line i18next/no-literal-string
export const Underline: IconComponent = makeCliIcon(symbol);
