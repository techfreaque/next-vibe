import type { IconComponent } from "../../../web/lib/helper";
import { makeCliIcon } from "./make-icon";

const symbol = "JS"; // eslint-disable-line i18next/no-literal-string
export const SiJavascript: IconComponent = makeCliIcon(symbol);
