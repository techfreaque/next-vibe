import type { IconComponent } from "../../../web/lib/helper";
import { makeCliIcon } from "./make-icon";

const symbol = "T"; // eslint-disable-line i18next/no-literal-string
export const SiTribe: IconComponent = makeCliIcon(symbol);
