import type { IconComponent } from "../../../web/lib/helper";
import { makeCliIcon } from "./make-icon";

const symbol = "Go"; // eslint-disable-line i18next/no-literal-string
export const SiGo: IconComponent = makeCliIcon(symbol);
