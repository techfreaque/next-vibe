import type { IconComponent } from "../../../web/lib/helper";
import { makeCliIcon } from "./make-icon";

const symbol = "阿"; // eslint-disable-line i18next/no-literal-string
export const SiAlibabadotcom: IconComponent = makeCliIcon(symbol);
