import type { IconComponent } from "../../../web/lib/helper";
import { makeCliIcon } from "./make-icon";

const symbol = "TS"; // eslint-disable-line i18next/no-literal-string
export const SiTypescript: IconComponent = makeCliIcon(symbol);
