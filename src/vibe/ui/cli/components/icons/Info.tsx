import type { IconComponent } from "../../../web/lib/helper";
import { makeCliIcon } from "./make-icon";

const symbol = "ℹ️"; // eslint-disable-line i18next/no-literal-string
export const Info: IconComponent = makeCliIcon(symbol);
