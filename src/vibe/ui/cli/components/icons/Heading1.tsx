import type { IconComponent } from "../../../web/lib/helper";
import { makeCliIcon } from "./make-icon";

const symbol = "H₁"; // eslint-disable-line i18next/no-literal-string
export const Heading1: IconComponent = makeCliIcon(symbol);
