import type { IconComponent } from "../../../web/lib/helper";
import { makeCliIcon } from "./make-icon";

const symbol = "小"; // eslint-disable-line i18next/no-literal-string
export const SiXiaomi: IconComponent = makeCliIcon(symbol);
