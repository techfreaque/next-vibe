import type { IconComponent } from "../../../web/lib/helper";
import { makeCliIcon } from "./make-icon";

const symbol = "Z"; // eslint-disable-line i18next/no-literal-string
export const SiZendesk: IconComponent = makeCliIcon(symbol);
