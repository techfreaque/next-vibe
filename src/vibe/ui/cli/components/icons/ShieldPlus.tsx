import type { IconComponent } from "../../../web/lib/helper";
import { makeCliIcon } from "./make-icon";

const symbol = "🛡"; // eslint-disable-line i18next/no-literal-string
export const ShieldPlus: IconComponent = makeCliIcon(symbol);
