import type { IconComponent } from "../../../web/lib/helper";
import { makeCliIcon } from "./make-icon";

const symbol = ">_"; // eslint-disable-line i18next/no-literal-string
export const Terminal: IconComponent = makeCliIcon(symbol);
