import type { IconComponent } from "../../../web/lib/helper";
import { makeCliIcon } from "./make-icon";

const symbol = "f"; // eslint-disable-line i18next/no-literal-string
export const Facebook: IconComponent = makeCliIcon(symbol);
