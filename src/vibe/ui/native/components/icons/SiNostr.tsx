// TODO: lucide-react-native doesn't have Nostr brand icon, using Zap as placeholder
import { Zap as SiNostrIcon } from "lucide-react-native";
import { styled } from "nativewind";

export const SiNostr = styled(SiNostrIcon, { className: "style" });
