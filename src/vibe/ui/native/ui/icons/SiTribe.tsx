// TODO: lucide-react-native doesn't have Tribe brand icon, using Users as placeholder
import { Users as SiTribeIcon } from "lucide-react-native";
import { styled } from "nativewind";

export const SiTribe = styled(SiTribeIcon, { className: "style" });
