/**
 * Shared constants for the story/marketing layout.
 * Kept in a separate file so client components can import without pulling in server-side code.
 */
export const SITE_FOOTER_ID = "site-footer";
