import { connectionTranslations } from "./connection";
import { syncTranslations } from "./sync";

export const imapErrorsTranslations = {
  connection: connectionTranslations,
  sync: syncTranslations,
};
