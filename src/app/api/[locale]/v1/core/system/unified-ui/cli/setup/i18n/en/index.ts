import { translations as installTranslations } from "../../install/i18n/en";
import { translations as statusTranslations } from "../../status/i18n/en";
import { translations as uninstallTranslations } from "../../uninstall/i18n/en";
import { translations as updateTranslations } from "../../update/i18n/en";

export const translations = {
  install: installTranslations,
  status: statusTranslations,
  uninstall: uninstallTranslations,
  update: updateTranslations,
};
