import { Text } from "ink";
import type { IconComponent } from "next-vibe/ui/web/lib/helper";
import type { JSX } from "react";

const symbol = "in"; // eslint-disable-line i18next/no-literal-string
export const Linkedin: IconComponent = (): JSX.Element => <Text>{symbol}</Text>;
