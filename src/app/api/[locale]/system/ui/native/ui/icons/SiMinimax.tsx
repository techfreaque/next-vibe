// TODO: lucide-react-native doesn't have MiniMax brand icon, using Activity as placeholder
import { Activity as SiMinimaxIcon } from "lucide-react-native";
import { styled } from "nativewind";

export const SiMinimax = styled(SiMinimaxIcon, { className: "style" });
