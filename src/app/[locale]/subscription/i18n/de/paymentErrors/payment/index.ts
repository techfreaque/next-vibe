import type { translations as enTranslations } from "../../../en/paymentErrors/payment";
import { translations as formTranslations } from "./form";

export const translations: typeof enTranslations = {
  form: formTranslations,
};
