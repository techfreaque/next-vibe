// TODO: lucide-react-native doesn't have Rumble brand icon, using Video as placeholder
import { Video as SiRumbleIcon } from "lucide-react-native";
import { styled } from "nativewind";

export const SiRumble = styled(SiRumbleIcon, { className: "style" });
