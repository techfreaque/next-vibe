// TODO: lucide-react-native doesn't have ByteDance brand icon, using Play as placeholder
import { Play as SiBytedanceIcon } from "lucide-react-native";
import { styled } from "nativewind";

export const SiBytedance = styled(SiBytedanceIcon, { className: "style" });
