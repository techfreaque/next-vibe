// CLI: calendar has no terminal equivalent — return null

import type { CalendarProps } from "../../web/ui/calendar";

export type { CalendarProps, DateRange } from "../../web/ui/calendar";

export function Calendar(_props: CalendarProps): null {
  void _props;
  return null;
}
