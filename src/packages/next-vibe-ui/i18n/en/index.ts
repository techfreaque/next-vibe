import { translations as webTranslations } from "../../web/i18n/en";
import { translations as nativeTranslations } from "../../native/i18n/en";

export const translations = {
  web: webTranslations,
  native: nativeTranslations,
};
