# ============================================================================
# AUTO-GENERATED FILE - DO NOT EDIT MANUALLY
# Generated by: vibe generate
# This file is auto-generated from environment module definitions.
# ============================================================================

# Source: src/browser/env.ts
# Absolute path to the Chrome/Chromium binary. Auto-detected from common install paths if unset.
# CHROME_EXECUTABLE_PATH="/usr/bin/chromium"
# Launch Chrome in headless mode (required for servers without a display). Default: false.
# CHROME_HEADLESS="true"
# Enable verbose Chrome DevTools MCP debug logging. Default: false.
# CHROME_MCP_DEBUG="true"
# Timeout in ms for Chrome MCP operations. Default: 120000.
# CHROME_MCP_TIMEOUT="120000"
# Custom Chrome user-data directory. Defaults to ~/.cache/chrome-devtools-mcp/chrome-profile.
# CHROME_USER_DATA_DIR="/tmp/chrome-profile"

# Source: src/contact/env-client.ts
NEXT_PUBLIC_SUPPORT_EMAIL_DE="hi@unbottled.ai"
NEXT_PUBLIC_SUPPORT_EMAIL_GLOBAL="hi@unbottled.ai"
NEXT_PUBLIC_SUPPORT_EMAIL_PL="hi@unbottled.ai"

# Source: src/env/env.ts
# Cron job secret - generate with: openssl rand -hex 32
CRON_SECRET="REPLACE_WITH_openssl_rand_hex_32_output"
# Database connection URL
DATABASE_URL="postgres://postgres:postgres@localhost:5432/postgres"
# Enable continuous file watching during development. When false (default), generators run once on startup and again when you press 'r' in the terminal - no CPU overhead. When true, generators re-run automatically on every file change. WARNING: continuous mode requires a high-end workstation (64 GB+ RAM, fast multi-core CPU). On less powerful machines it will cause noticeable slowdowns and may make the dev server unresponsive.
DEV_WATCHER_CONTINUOUS="false"
# JWT signing secret - MUST be at least 64 random characters in production. Generate with: openssl rand -hex 32
JWT_SECRET_KEY="REPLACE_WITH_openssl_rand_hex_32_output"
# Preview database port for local mode (vibe build/start). Derives DATABASE_URL by swapping the port.
PREVIEW_DB_PORT="5433"
# Preview app port for local mode (vibe build/start). Derives NEXT_PUBLIC_APP_URL by swapping the port.
PREVIEW_PORT="3001"
# Pulse runner interval in minutes (default: 1)
PULSE_INTERVAL_MINUTES="1"
# CLI locale setting
VIBE_CLI_LOCALE="en-GLOBAL"
# Registry + image name the prod Docker image is built and pushed to (see: vibe image-push). install-docker.sh pulls this same image on the deploy server instead of building it there.
# DOCKER_IMAGE_NAME="ghcr.io/techfreaque/next-vibe-app"
# Enable verbose debug logging across the full stack, including the browser client. Two ways to activate: (1) pass -v or --verbose to any vibe command - detected at startup and baked into the build automatically; (2) set NEXT_PUBLIC_VIBE_DEBUG=true in .env for a persistent always-on debug build.
# NEXT_PUBLIC_VIBE_DEBUG="false"
# Absolute path to the project root. Mainly needed for MCP servers which often run in a different working directory.
# PROJECT_ROOT="/path/to/your/project"
# Redis connection URL. Required when WS_PUBSUB_TYPE=redis for cross-instance WebSocket broadcasting.
# REDIS_URL="redis://localhost:6379"
# Default SSH target for `vibe image-push` when --sshTarget isn't passed explicitly. Format: user@host or user@host:port.
# SSH_SERVER="root@203.0.113.5"
# Password for SSH_SERVER (or an explicit --sshTarget). If unset, `vibe image-push` falls back to the system ssh client (key/agent auth). Only used for the image transfer - never logged.
# SSH_SERVER_PWD="REPLACE_WITH_your_ssh_password"
# Root admin email. Used for CLI auth, API tool access, and all admin endpoints. Change via the app syncs to DB.
# VIBE_ADMIN_USER_EMAIL="admin@please.change.me"
# Root admin password. Protects all exposed tools/endpoints. Use a strong password in production! Change via the app syncs to DB.
# VIBE_ADMIN_USER_PASSWORD="your-admin-password"
# Opt out of the built-in Bun HTTP+WebSocket proxy. When true: Next.js runs directly on the main port and the WebSocket sidecar runs on port+1000. Use this when you have your own reverse proxy (Caddy, nginx, etc.) that already forwards /ws to the sidecar port. Your reverse proxy must route /ws → port+1000 and everything else → main port.
# VIBE_DISABLE_PROXY="false"
# Directory for server log files (.atlas.log, .hermes.log, .hermes-dev.log, .vibe-mcp.log). Only used when VIBE_LOG_TARGET=file. Defaults to .tmp. Set to an absolute or relative path to change location.
# VIBE_LOG_PATH=".tmp"
# Log persistence target. Auto-resolved from VIBE_SERVER_MODE if not set: atlas-dev/hermes-dev/mcp→file, hermes-prod→db. Values: 'file' (write to VIBE_LOG_PATH log files), 'db' (errors+warnings to error_logs table only, no file), 'none' (no persistence).
# VIBE_LOG_TARGET="file"
# Remote server URL for `vibe --thea` CLI execution.
# VIBE_REMOTE_URL="https://unbottled.ai"
# WebSocket pub/sub adapter. 'local' for single-instance (default), 'redis' for multi-instance broadcasting.
# WS_PUBSUB_TYPE="local"

# Source: src/env/env-client.ts
NEXT_PUBLIC_APP_URL="http://localhost:3000"
NEXT_PUBLIC_DEBUG_PRODUCTION="false"
NEXT_PUBLIC_LOCAL_MODE="false"
NEXT_PUBLIC_TEST_SERVER_URL="http://localhost:4000"
NEXT_PUBLIC_VIBE_IS_CLOUD="false"
NODE_ENV="development"
# Project URL - defaults to https://unbottled.ai. Override to use your own domain.
# NEXT_PUBLIC_PROJECT_URL="https://unbottled.ai"

# Source: src/leads/campaigns/env.ts
# Lead campaigns SMTP
# LEADS_EMAIL_FROM_EMAIL="leads@example.com"
# Display name in the From header for lead campaign emails
# LEADS_EMAIL_FROM_NAME="Unbottled Team"
# LEADS_EMAIL_HOST="smtp.example.com"
# LEADS_EMAIL_PASS="leads_email_password"
# LEADS_EMAIL_PORT="587"
# LEADS_EMAIL_SECURE="true"
# LEADS_EMAIL_USER="leads_email_username"
# Lead campaigns IMAP (inbound) - leave blank if same host as SMTP
# LEADS_IMAP_HOST="imap.example.com"
# LEADS_IMAP_PASS="leads_imap_password"
# LEADS_IMAP_PORT="993"
# LEADS_IMAP_SECURE="true"
# LEADS_IMAP_USER="leads_imap_username"

# Source: src/messenger/env.ts
# EMAIL_FROM_EMAIL="noreply@example.com"
# Display name in the From header for system emails
# EMAIL_FROM_NAME="Unbottled"
# EMAIL_HOST="smtp.example.com"
# EMAIL_PASS="your_email_password"
# EMAIL_PORT="587"
# EMAIL_SECURE="true"
# EMAIL_USER="your_email_username"
# System IMAP (inbound) - leave blank if same host as SMTP
# IMAP_HOST="imap.example.com"
# IMAP_PASS="your_imap_password"
# IMAP_PORT="993"
# IMAP_SECURE="true"
# IMAP_USER="your_imap_username"

# Source: src/messenger/providers/email/imap-client/env.ts
# IMAP seeding config
# IMAP_SEED_ACCOUNT_NAME="Your IMAP Account"
# IMAP_SEED_EMAIL="your-email@example.com"
# IMAP_SEED_HOST="imap.example.com"
# IMAP_SEED_PASSWORD="your-imap-password"
# IMAP_SEED_PORT="993"
# IMAP_SEED_SECURE="true"
# IMAP_SEED_USERNAME="your-email@example.com"

# Source: src/payment/env.ts
# NOWPayments (crypto)
# NOWPAYMENTS_API_KEY="your_nowpayments_api_key"
# NOWPayments API endpoint - sandbox for testing, production for live payments
# NOWPAYMENTS_API_URL="https://api-sandbox.nowpayments.io/v1"
# NOWPAYMENTS_IPN_SECRET="your_nowpayments_ipn_secret"
# Stripe
# STRIPE_SECRET_KEY="sk_test_your_stripe_secret_key"
# STRIPE_WEBHOOK_SECRET="whsec_your_stripe_webhook_secret"

# Source: src/payment/env-client.ts
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_your_stripe_publishable_key"

# Source: src/sms/env.ts
# ADMIN_NOTIFICATION_PHONE="+1234567890"
# AWS SNS provider
# AWS_ACCESS_KEY_ID="your_aws_access_key"
# AWS_REGION="us-east-1"
# AWS_SECRET_ACCESS_KEY="your_aws_secret_key"
# MessageBird provider
# MESSAGEBIRD_ACCESS_KEY="your_messagebird_key"
# SMS_FROM_NUMBER="+1234567890"
# SMS_HTTP_API_KEY="your_api_key"
# SMS_HTTP_API_METHOD="POST"
# HTTP SMS provider
# SMS_HTTP_API_URL="https://api.example.com/sms"
# SMS_HTTP_AUTH_SCHEME="Bearer"
# SMS_HTTP_CONTENT_TYPE="application/json"
# SMS_HTTP_CUSTOM_HEADERS="{"X-Custom": "value"}"
# SMS_HTTP_FROM_FIELD="from"
# SMS_HTTP_MESSAGE_FIELD="message"
# SMS_HTTP_PHONE_REGEX="^\+[1-9]\d{1,14}$"
# SMS_HTTP_RESPONSE_ID_FIELD="id"
# SMS_HTTP_TO_FIELD="to"
# SMS_MAX_LENGTH="160"
# SMS_MAX_RETRY_ATTEMPTS="3"
# SMS/messaging provider
# SMS_PROVIDER="twilio"
# SMS_RETRY_DELAY_MS="1000"
# Telegram Bot provider - token from BotFather
# TELEGRAM_BOT_TOKEN="123456789:AAFxxxxxxxx"
# Twilio provider
# TWILIO_ACCOUNT_SID="your_twilio_account_sid"
# TWILIO_AUTH_TOKEN="your_twilio_auth_token"
# TWILIO_REGION="us1"
# WhatsApp Business provider - permanent access token
# WHATSAPP_ACCESS_TOKEN="EAAxxxxxxxx"
# WhatsApp Business provider - phone number ID from Meta dashboard
# WHATSAPP_PHONE_NUMBER_ID="123456789012345"

# Source: src/vibe/agent/env.ts
# Filesystem path for chat data
CHAT_STORAGE_PATH="./chat-data"
# Storage type - Filesystem
CHAT_STORAGE_TYPE="filesystem"
# Brave Search API key - get yours at https://api.search.brave.com/app/keys
# BRAVE_SEARCH_API_KEY="your-brave-search-key"
# Claude Code provider enabled - set true/false to override, or leave unset for auto-detection (checks if `claude` CLI is installed)
# CLAUDE_CODE_ENABLED="true"
# Deepgram API key for fast, accurate STT - get yours at https://console.deepgram.com
# DEEPGRAM_API_KEY="your-deepgram-key"
# Eden AI API key (voice/TTS) - get yours at https://app.edenai.run/user/settings#api
# EDEN_AI_API_KEY="your-eden-ai-key"
# ElevenLabs API key for high-quality TTS voices - get yours at https://elevenlabs.io/app/settings/api-keys
# ELEVENLABS_API_KEY="your-elevenlabs-key"
# Fal.ai API key for fast image and video generation - get yours at https://fal.ai/dashboard/keys
# FAL_AI_API_KEY="your-fal-ai-key"
# FreedomGPT API key
# FREEDOMGPT_API_KEY="your-freedomgpt-key"
# Gab AI API key
# GAB_AI_API_KEY="your-gab-ai-key"
# Kagi API key - get yours at https://kagi.com/settings?p=api
# KAGI_API_KEY="your-kagi-api-key"
# ModelsLab API key for music generation and text-to-video - get yours at https://modelslab.com/account/api
# MODELSLAB_API_KEY="your-modelslab-key"
# OpenAI API key for DALL-E and gpt-image models - get yours at https://platform.openai.com/api-keys
# OPENAI_API_KEY="sk-..."
# OpenRouter API key - access 200+ AI models. Get yours at https://openrouter.ai/keys
# OPENROUTER_API_KEY="sk-or-v1-..."
# Replicate API token for Flux Pro, SDXL, and video generation models - get yours at https://replicate.com/account/api-tokens
# REPLICATE_API_TOKEN="r8_..."
# Not used with filesystem storage
# S3_ACCESS_KEY_ID="AKIAIOSFODNN7EXAMPLE"
# Not used with filesystem storage
# S3_BUCKET="my-bucket"
# Not used with filesystem storage
# S3_ENDPOINT="https://s3.amazonaws.com"
# Not used with filesystem storage
# S3_PUBLIC_URL_BASE="https://cdn.example.com"
# Not used with filesystem storage
# S3_REGION="us-east-1"
# Not used with filesystem storage
# S3_SECRET_ACCESS_KEY="wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY"
# Scrappey API key
# SCRAPPEY_API_KEY="your-scrappey-key"
# Uncensored AI API key
# UNCENSORED_AI_API_KEY="your-uncensored-ai-key"
# Venice AI API key - get yours at https://venice.ai
# VENICE_AI_API_KEY="your-venice-ai-key"
# AI model to use in integration tests. Defaults to claude-code-haiku when CLAUDE_CODE_ENABLED resolves true, otherwise the platform default chat model.
# VIBE_TEST_AI_MODEL="claude-code-haiku"
